# Files permitted to call ArcSwap::store. See the single-snapshot-publish
# rule in scripts/invariant-lints.sh for why this list should normally hold
# exactly one entry: ArcSwap::store may be called in exactly one function in
# the whole workspace, because a second publication site reintroduces torn
# configuration (a request could see a route from generation N and a filter
# chain from N+1).
#
# One repository-relative path per file, exactly as it appears in `git
# ls-files`. Matching is EXACT path equality only: never a prefix, a suffix,
# a glob, or a directory. Blank lines and lines starting with `#` are
# ignored. Every entry must be justified with a comment on the line above it
# explaining why that specific file is the designated publisher, added in
# the same PR that needs it.
#
# Empty in M1: no ArcSwap exists yet, so this ships empty on purpose, ahead
# of the config plane that will need it.
#
# TlsMaterialCell, the third allowlisted ArcSwap declaration site (decision-ledger entry 42's
# amendment, cert-index-incremental-rebuild-and-publish #118): read once per accepted connection,
# before any request exists, so it cannot tear the route-to-filter relationship the single-
# snapshot rule protects, and TLS material changes on ACME issuance and OCSP staple refresh,
# neither of which is a configuration change. ArcSwap::store appears in exactly one function,
# TlsMaterialCell::publish, both defined in this one file.
crates/irontraffic-tls/src/store/publish.rs
#
# Not an ArcSwap publisher (issue #407, bench-provenance-capture): the one
# `.store(` in this file is `AtomicBool::store` on a plain `Arc<AtomicBool>`
# flag two bounded-probe drain threads use to tell the main thread a byte
# cap was exceeded. There is no ArcSwap, no config snapshot, and no torn-read
# hazard this rule protects against here; it is caught only because the rule
# is deliberately over-broad (see the rule's own header comment).
crates/irontraffic-bench/src/provenance.rs
