# SPDX-License-Identifier: MIT OR Apache-2.0
#
# The libraries the `x86_64-unknown-linux-gnu` and `aarch64-unknown-linux-gnu`
# release binaries are allowed to link dynamically, one SONAME per line. The
# `release-artifacts` CI job compares each binary's actual `ldd` (or, for the
# cross-compiled aarch64 target, `readelf -d`'s `NEEDED` entries) output
# against this file and fails on anything not listed, so a new C dependency
# entering the tree cannot silently start shipping in the dynamically linked
# release artifacts. See docs/RELEASE.md.
#
# As of this file, `crates/irontraffic` has no C dependency of its own (no
# TLS, no zstd; see docs/RELEASE.md's "What this table does not yet say"
# section), so every entry below comes from the glibc toolchain itself, not
# from anything this workspace added.
linux-vdso.so.1
libgcc_s.so.1
libc.so.6
libm.so.6
ld-linux-x86-64.so.2
ld-linux-aarch64.so.1
